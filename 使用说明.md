# 开始使用墨格创作室

双击「墨格创作室.exe」，进入独立桌面窗口。

1. 左下角「生成连接」→ 保持「ChatGPT 订阅 · 官方 Codex」→「保存并检测连接」。如未登录，点击「使用 ChatGPT 登录」，在官方网页完成登录。
2. 「新建短篇」→ 输入创作思路或小说片段 → 选择格数与画风 →「生成故事纲要」。
3. 修改纲要、确认方向；随后可生成小说文稿、固定角色并制作参考图。
4. 「分镜绘制」→ 先生成脚本，检查后绘图。支持逐格重绘、挑选旧版本和失败后补绘。
5. 「排版导出」→ 调整布局、中文气泡与旁白 → 导出 PNG、PDF 或可编辑工程。

默认不需要 API 密钥，文字与插画都通过本机官方 Codex 使用你的 ChatGPT 账号。额度不足时等待恢复即可，不会自动转用 API 计费。连接窗口可查看套餐与剩余额度。

本机已安装 Codex；换电脑时先安装官方 Codex CLI，再登录自己的 ChatGPT 账号。可通过 `npm install -g @openai/codex` 安装。程序通常自动发现 Codex，也可在「Codex 程序路径」中指定官方 codex.exe。

只有主动选择「OpenAI API · 单独按量计费」时才需要 API Key。密钥从环境变量或本机加密设置读取，不包含在源码、GitHub 或作品导出中。

作品自动保存在本机。右上角出现「已保存到本机」后再关闭。生成中退出会中断当前任务；已完成结果会保留。需要回到旧稿时点击「历史」。

完整说明与数据备份位置见 README.md。

日常也可双击「墨格创作室（快速启动）」，直接打开已展开的桌面程序。若使用这一入口，请保留 release/win-unpacked 文件夹。
